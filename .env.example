# Porta host del frontend (nginx ascolta su 4200 nel container) -> http://host:8181
FRONTEND_PORT=8181

# Versione delle immagini Docker (tag)
VERSION=1.0.0

# JDK version per la build del backend (21 = LTS consigliato, 25 se disponibile)
JDK_VERSION=21
